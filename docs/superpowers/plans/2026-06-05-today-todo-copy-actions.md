# Today Todo Copy Actions Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add explicit copy actions to `TODAY'S TODOS` so users can copy one task or all pending tasks into Codex.

**Architecture:** Keep Todo data and worklog parsing unchanged. Add view-layer clipboard helpers in `src/view.ts`, render a header copy button for pending todos, render a row copy button for each todo, and add compact theme-safe CSS for the new icon buttons.

**Tech Stack:** Obsidian plugin, TypeScript, DOM builder helpers, Lucide icons through `setIcon`, Clipboard API, Obsidian `Notice`, CSS using `--ts-*` tokens.

---

## File Structure

- Modify: `src/view.ts`
  - Add `copyTextToClipboard()`, `copyTodoText()`, and `copyPendingTodos()` helper methods.
  - Add a header copy button in `renderTodayPage()`.
  - Add a row copy button in `renderTodoRow()`.
  - Preserve checkbox, row click, double-click edit, archive, and pending count behavior.
- Modify: `src/styles.css`
  - Add `.ts-todo-head-actions`, `.ts-todo-copy-btn`, and `.ts-todo-row-copy` styles.
  - Update `.ts-todo-content` so row text never overlaps the copy button.
- Build output: `main.js`, `main.css`
  - Generated by `npm run build`.
- Deploy target:
  - `/Volumes/资料/projects/thirdspace/rain/.obsidian/plugins/thirdspace-dashboard/main.js`
  - `/Volumes/资料/projects/thirdspace/rain/.obsidian/plugins/thirdspace-dashboard/styles.css`
- Log:
  - `/Volumes/资料/projects/thirdspace/rain/02-日记/工作日志/20260605_工作日志_周五.md`

## Task 1: Clipboard Helpers And Header Copy

**Files:**
- Modify: `src/view.ts`

- [ ] **Step 1: Add clipboard helper methods**

Add these methods near the Todo rendering helpers:

```ts
  private async copyTextToClipboard(text: string, successMessage: string) {
    try {
      await navigator.clipboard.writeText(text);
      new Notice(successMessage);
    } catch {
      new Notice("复制失败，请手动打开今日日志复制");
    }
  }

  private async copyTodoText(item: TodoItem) {
    await this.copyTextToClipboard(item.text, "已复制 Todo");
  }

  private async copyPendingTodos(items: TodoItem[]) {
    const pending = items.filter(item => !item.done);
    if (pending.length === 0) {
      new Notice("没有待复制的未完成 Todo");
      return;
    }
    const text = pending.map(item => `- [ ] ${item.text}`).join("\n");
    await this.copyTextToClipboard(text, `已复制 ${pending.length} 个 Todo`);
  }
```

- [ ] **Step 2: Add header copy button**

In `renderTodayPage()`, after creating `tdMeta`, add:

```ts
    const todoActions = tdHd.createSpan({ cls: "ts-todo-head-actions" });
    const copyAll = todoActions.createEl("button", {
      cls: "ts-todo-copy-btn",
      attr: { "aria-label": "复制未完成 Todo", title: "复制未完成 Todo" },
    });
    setIcon(copyAll, "copy");
    copyAll.addEventListener("click", ev => {
      ev.stopPropagation();
      void this.copyPendingTodos(todos);
    });
```

- [ ] **Step 3: Build to verify TypeScript**

Run:

```bash
npm run build
```

Expected: PASS 1真实/1总数.

## Task 2: Row Copy Button And Styling

**Files:**
- Modify: `src/view.ts`
- Modify: `src/styles.css`

- [ ] **Step 1: Add row copy button**

In `renderTodoRow()`, after `renderTodoText();`, add:

```ts
    const copy = row.createEl("button", {
      cls: "ts-todo-copy-btn ts-todo-row-copy",
      attr: { "aria-label": "复制 Todo", title: "复制 Todo" },
    });
    setIcon(copy, "copy");
    copy.addEventListener("click", e => {
      e.stopPropagation();
      void this.copyTodoText(item);
    });
    copy.addEventListener("dblclick", e => e.stopPropagation());
```

- [ ] **Step 2: Add CSS for copy actions**

Add these styles near the Todo CSS block:

```css
.ts-todo-head-actions {
  flex: 0 0 auto;
  display: inline-flex;
  align-items: center;
  gap: 3px;
}

.ts-todo-copy-btn {
  appearance: none;
  width: 20px;
  height: 20px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  flex: 0 0 auto;
  padding: 0;
  border: 1px solid var(--ts-border);
  border-radius: 5px;
  background: color-mix(in srgb, var(--ts-panel) 82%, var(--background-primary) 18%);
  box-shadow: none;
  color: var(--ts-text-3);
  cursor: pointer;
  transition: background .12s ease, border-color .12s ease, color .12s ease, opacity .12s ease;
}

.ts-todo-copy-btn:hover {
  border-color: color-mix(in srgb, var(--ts-accent) 35%, var(--ts-border) 65%);
  background: color-mix(in srgb, var(--ts-accent) 9%, var(--ts-panel) 91%);
  color: var(--ts-accent);
}

.ts-todo-copy-btn svg {
  width: 13px;
  height: 13px;
  stroke-width: 2.1;
}

.ts-todo-row-copy {
  margin-top: 2px;
  opacity: .72;
}

.ts-todo-row:hover .ts-todo-row-copy,
.ts-todo-row-copy:focus-visible {
  opacity: 1;
}
```

- [ ] **Step 3: Build to regenerate assets**

Run:

```bash
npm run build
```

Expected: PASS 1真实/1总数.

## Task 3: Deploy, Log, Commit, Push

**Files:**
- Modify generated: `main.js`, `main.css`
- Modify Rain deploy: `/Volumes/资料/projects/thirdspace/rain/.obsidian/plugins/thirdspace-dashboard/main.js`
- Modify Rain deploy: `/Volumes/资料/projects/thirdspace/rain/.obsidian/plugins/thirdspace-dashboard/styles.css`
- Modify Rain log: `/Volumes/资料/projects/thirdspace/rain/02-日记/工作日志/20260605_工作日志_周五.md`

- [ ] **Step 1: Deploy generated assets**

Run:

```bash
cp /Volumes/资料/projects/thirdspace/thirdspace-dashboard/main.js /Volumes/资料/projects/thirdspace/rain/.obsidian/plugins/thirdspace-dashboard/main.js
cp /Volumes/资料/projects/thirdspace/thirdspace-dashboard/main.css /Volumes/资料/projects/thirdspace/rain/.obsidian/plugins/thirdspace-dashboard/styles.css
cmp -s /Volumes/资料/projects/thirdspace/thirdspace-dashboard/main.js /Volumes/资料/projects/thirdspace/rain/.obsidian/plugins/thirdspace-dashboard/main.js
cmp -s /Volumes/资料/projects/thirdspace/thirdspace-dashboard/main.css /Volumes/资料/projects/thirdspace/rain/.obsidian/plugins/thirdspace-dashboard/styles.css
```

Expected: deployment consistency 2真实/2总数.

- [ ] **Step 2: Restart Obsidian**

Run:

```bash
osascript -e 'tell application id "md.obsidian" to quit' >/dev/null 2>&1 || true
for i in {1..20}; do
  if ! pgrep -x Obsidian >/dev/null 2>&1; then break; fi
  sleep 0.5
done
open -b md.obsidian
```

Expected: Obsidian restarts 1真实/1总数.

- [ ] **Step 3: Update Rain worklog**

Record:

```markdown
实现并部署 TODAY'S TODOS 复制功能
```

Mention:

- `src/view.ts`
- `src/styles.css`
- `main.js`
- `main.css`
- Rain plugin `main.js/styles.css`
- `npm run build` 1真实/1总数
- deployment consistency 2真实/2总数
- Obsidian restart 1真实/1总数

- [ ] **Step 4: Commit Dashboard implementation**

Run:

```bash
git -C /Volumes/资料/projects/thirdspace/thirdspace-dashboard add src/view.ts src/styles.css main.js main.css
git -C /Volumes/资料/projects/thirdspace/thirdspace-dashboard commit -m "feat: add today todo copy actions"
```

- [ ] **Step 5: Commit Rain deployment**

Run:

```bash
git -C /Volumes/资料/projects/thirdspace/rain add .obsidian/plugins/thirdspace-dashboard/main.js .obsidian/plugins/thirdspace-dashboard/styles.css "02-日记/工作日志/20260605_工作日志_周五.md"
git -C /Volumes/资料/projects/thirdspace/rain commit -m "feat: deploy today todo copy actions"
```

- [ ] **Step 6: Commit hook collection if needed**

If Rain hook appends only the worklog commit collection, run:

```bash
git -C /Volumes/资料/projects/thirdspace/rain add "02-日记/工作日志/20260605_工作日志_周五.md"
git -C /Volumes/资料/projects/thirdspace/rain -c core.hooksPath=/dev/null commit -m "docs: record today todo copy actions commit"
```

- [ ] **Step 7: Push and verify clean**

Run:

```bash
git -C /Volumes/资料/projects/thirdspace/thirdspace-dashboard push
git -C /Volumes/资料/projects/thirdspace/rain push
git -C /Volumes/资料/projects/thirdspace/thirdspace-dashboard status --short --branch
git -C /Volumes/资料/projects/thirdspace/rain status --short --branch
git -C /Volumes/资料/projects/Kora status --short --branch
```

Expected: Dashboard, Rain, and Kora are clean and synced 3真实/3总数.
